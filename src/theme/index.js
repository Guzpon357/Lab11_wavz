import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#292926",
    },
    secondary: {
      main: "#1d2e17",
    },
    success: {
      main: "#5fc3cf",
    },
  },
});

export default theme;